@model IEnumerable<Yemek_Poşeti.Models.OrderDetailModel>

@{
    ViewBag.Title = "OrderDetailListele";
    Layout = "~/Views/Shared/_LayoutAdminYönetici.cshtml";
}

<h2>OrderDetailListele</h2>

<p>
    @Html.ActionLink("Create New", "OrderDetailEY")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrderDetailID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Introduction)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrderDetailID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Introduction)
        </td>
        <td>
            @Html.ActionLink("Edit", "OrderDetailEY", new {  id=item.OrderDetailID  }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.OrderDetailID  })
        </td>
    </tr>
}

</table>
